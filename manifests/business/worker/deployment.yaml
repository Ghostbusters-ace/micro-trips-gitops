apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: micro-trips
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - name: worker
        image: ghcr.io/ghostbusters-ace/worker:v1.0.8
        env:
        - name: RABBITMQ_HOST
          value: "rabbitmq.storage-messaging.svc.cluster.local"
        resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"